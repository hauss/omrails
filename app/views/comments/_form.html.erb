<%= form_for([@pin, @pin.comments.build]) do |f| %>
  <div class="field">
    <%= f.text_area :body, :rows => "3", :placeholder => "Enter your comment" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>